import {
  mockSelectedFeature,
} from '../mocks/mockSelectedFeature'

export const mockIdentifyFeature =  // Función que simula la identificación de una característica en el mapa y devuelve un objeto de característica mock
  () => {

    return (
      mockSelectedFeature
    )
  }
